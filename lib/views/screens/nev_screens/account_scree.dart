import 'package:flutter/material.dart';

class AccountScree extends StatelessWidget {
  const AccountScree({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Account")));
  }
}
